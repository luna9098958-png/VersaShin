# Shared System Manual

This documentation file is located at the standard `/usr/local/share` directory structure.
In Unix-like systems, `/usr/local/share` is used for architecture-independent shared data files.
